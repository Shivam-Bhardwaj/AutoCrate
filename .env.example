# ===================================
# AutoCrate Security Configuration
# ===================================
# Copy this file to .env.local (or appropriate env file) and provide real values.

# Application URL
NEXT_PUBLIC_APP_URL=http://localhost:3000
NODE_ENV=development

# ===================================
# Authentication Configuration (REQUIRED)
# ===================================

# JWT Secret Key (Generate a strong random string in production)
# Example: openssl rand -base64 32
JWT_SECRET=change-this-to-a-secure-random-string-in-production

# Console Password Hash (Use bcrypt to hash passwords)
# Default in code: bcrypt hash of "default-console-password"
# Generate: node -e "console.log(require('bcryptjs').hashSync('your-password', 10))"
CONSOLE_PASSWORD_HASH=$2a$10$CHANGE_THIS_TO_YOUR_BCRYPT_HASH

# Terminal Password Hash (Use bcrypt to hash passwords)
# Default in code: bcrypt hash of "default-terminal-password"
# Generate: node -e "console.log(require('bcryptjs').hashSync('your-password', 10))"
TERMINAL_PASSWORD_HASH=$2a$10$CHANGE_THIS_TO_YOUR_BCRYPT_HASH

# ===================================
# Deployment Configuration
# ===================================

# Deployment automation (used by scripts/deploy.sh)
VERCEL_TOKEN=change-me

# ===================================
# Legacy Client-Side Auth (DEPRECATED)
# ===================================
# These are replaced by server-side auth above but kept for backward compatibility
# DO NOT USE FOR NEW DEPLOYMENTS

# Password gate for /terminal (client-side use only) - DEPRECATED
NEXT_PUBLIC_TERMINAL_PASSWORD=change-me

# Password gate for /console (client-side use only) - DEPRECATED
NEXT_PUBLIC_CONSOLE_PASSWORD=change-me
